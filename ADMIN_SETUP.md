# Админ Үүсгэх Заавар

## 🎯 Хамгийн Хялбар Арга: Browser Console

### Алхам 1: Нэвтрэх

1. App дээр нэвтрэх (`/login`)
2. Бүртгүүлсэн хэрэглэгчээр нэвтрэх (админ болгох хэрэглэгч)

### Алхам 2: Browser Console нээх

1. **F12** дарах (эсвэл баруун товч → Inspect)
2. **Console** таб сонгох

### Алхам 3: Админ болгох

Console дээр дараах командыг ажиллуулна:

```javascript
await window.makeCurrentUserAdmin()
```

### Алхам 4: Хуудас Refresh хийх

1. **F5** дарах (эсвэл хуудас refresh хийх)
2. Одоо админ цэс харагдах ёстой

---

## ✅ Шалгах

Админ болсны дараа:

1. **"Admin"** цэс харагдах ёстой (header дээр)
2. `/admin` хуудас руу орох боломжтой
3. Бизнес, машины заруудыг баталгаажуулах/устгах боломжтой

---

## 🔄 Админыг Арилгах (USER болгох)

Хэрэв админыг арилгах хэрэгтэй бол:

### Одоогийн хэрэглэгчийг USER болгох:

```javascript
await window.removeAdminFromCurrentUser()
```

### Тодорхой хэрэглэгчийг USER болгох:

```javascript
await window.removeAdminFromUser('user-uid-here')
```

**UID олох:**
- Firebase Console → Authentication → Users таб дээр харна
- Эсвэл browser console дээр: `JSON.parse(localStorage.getItem('user')).uid`

---

## 🔧 Арга 2: Firebase Console (Хэрэв Browser Console ажиллахгүй бол)

### Алхам 1: User UID олох

1. Firebase Console → Authentication → Users таб руу орох
2. Админ болгох хэрэглэгчийг олох
3. UID-г хуулна

Эсвэл Browser Console дээр:

```javascript
JSON.parse(localStorage.getItem('user')).uid
```

### Алхам 2: Firestore дээр User Document үүсгэх/засах

1. Firebase Console → Firestore Database руу орох
2. `users` collection үүсгэх (хэрэв байхгүй бол)
3. User-ийн UID-тай document үүсгэх (эсвэл одоогийн document-ийг засах)
4. Дараах field-ууд нэмэх/засах:
   - `email`: "user@example.com" (хэрэглэгчийн email)
   - `role`: "ADMIN"
   - `full_name`: "Admin User" (сонголттой)
   - `created_at`: (timestamp, сонголттой)

### Алхам 3: Хуудас Refresh хийх

1. App дээр хуудас refresh хийх
2. Одоо админ эрхтэй байх ёстой

---

## ⚠️ Чухал Тайлбар

- **Зөвхөн нэг админ байх боломжтой!**
- Хэрэв админ аль хэдийн байгаа бол, шинэ админ үүсгэхээсээ өмнө одоогийн админыг USER болгох хэрэгтэй
- Админ үүсгэсний дараа хуудас refresh хийх шаардлагатай

---

## 🆘 Асуудал гарвал

### Админ цэс харагдахгүй байна:

1. Хуудас refresh хийх (F5)
2. Browser cache цэвэрлэх (Ctrl + Shift + Delete)
3. Console дээр алдаа байгаа эсэхийг шалгах

### "Админ аль хэдийн байна" гэсэн алдаа:

1. Одоогийн админыг олох:
   ```javascript
   // Firestore дээр админ хайх
   ```
2. Одоогийн админыг USER болгох:
   ```javascript
   await window.removeAdminFromUser('existing-admin-uid')
   ```
3. Дахин админ үүсгэх:
   ```javascript
   await window.makeCurrentUserAdmin()
   ```

---

## 📝 Тест хийх

Админ болсны дараа:

1. ✅ `/admin` хуудас руу орох боломжтой эсэх
2. ✅ Бизнес, машины заруудыг харах боломжтой эсэх
3. ✅ Заруудыг баталгаажуулах/устгах боломжтой эсэх
4. ✅ "Admin" цэс header дээр харагдаж байгаа эсэх

