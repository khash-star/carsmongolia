# Админ Хуудас руу Орохгүй Байвал - Засах Заавар

## 🔴 Асуудал
Админ хуудас (`/Admin`) руу орохгүй байна.

## ✅ Шийдэл

### Алхам 1: User Role Шалгах

Browser Console (F12) дээр:

```javascript
// User role шалгах
await checkUserRole()
```

Энэ нь:
- Одоогийн user role-г харуулна
- Firestore дээрх role-г харуулна
- localStorage дээрх role-г харуулна

### Алхам 2: Хэрэв USER role-тэй байвал ADMIN болгох

```javascript
// Одоогийн хэрэглэгчийг ADMIN болгох
await window.makeCurrentUserAdmin()

// Хуудас refresh хийх
window.location.reload()
```

### Алхам 3: Хуудас Refresh хийх

1. **F5** дарах (эсвэл хуудас refresh)
2. Одоо "Админ" цэс харагдах ёстой
3. "Админ" цэс дээр дарах эсвэл `/Admin` руу орох

---

## 🔍 Шалгах Алхмууд

### 1. Админ Цэс Харагдаж байгаа эсэх

Header дээр "Админ" цэс харагдаж байгаа эсэхийг шалгах.

Хэрэв харагдахгүй бол:
- User role зөв биш байж магадгүй
- `checkUserRole()` ажиллуулж шалгах

### 2. Админ Хуудас руу Орох

1. `/Admin` URL руу шууд орох
2. Эсвэл "Админ" цэс дээр дарах

Хэрэв `/` руу redirect хийвэл:
- User role зөв биш байна
- `makeCurrentUserAdmin()` ажиллуулж ADMIN болгох

### 3. Console дээр Алдаа Шалгах

Browser Console (F12) дээр:
- "Admin access denied" гэсэн warning харагдах ёстой
- User role-г харуулна

---

## 🆘 Хэрэв бүх зүйл зөв байгаа ч ажиллахгүй байвал

### 1. Browser Cache Цэвэрлэх

1. **Ctrl + Shift + Delete** дарах
2. "Cached images and files" сонгох
3. "Clear data" дарах
4. Браузер дахин ачаалах

### 2. localStorage Цэвэрлэх

Browser Console дээр:

```javascript
// localStorage цэвэрлэх
localStorage.clear()

// Дахин нэвтрэх
window.location.href = '/login'
```

### 3. User Role Дахин Тохируулах

```javascript
// User role шалгах
await checkUserRole()

// Хэрэв USER байвал ADMIN болгох
await window.makeCurrentUserAdmin()

// Хуудас refresh
window.location.reload()
```

---

## 📝 Чухал Тайлбар

- User role-г Firestore дээр `users/{uid}` document дээр `role: "ADMIN"` гэж тохируулах шаардлагатай
- localStorage дээрх user object-д role байх ёстой
- Хуудас refresh хийсний дараа админ цэс харагдах ёстой

---

## ✅ Тест хийх

1. Нэвтрэх (`/login`)
2. Browser Console дээр `checkUserRole()` ажиллуулах
3. Хэрэв USER role-тэй байвал `makeCurrentUserAdmin()` ажиллуулах
4. Хуудас refresh хийх (F5)
5. "Админ" цэс харагдах ёстой
6. `/Admin` хуудас руу орох боломжтой байх ёстой

