# Нэвтрэх Асуудал Засах

## 🔴 Асуудал
Нууц үг солигдсон ч нэвтрэх дараад ажиллахгүй байна.

## ✅ Шийдэл

### 1. Browser Cache Цэвэрлэх

**Хэт олон оролдлого алдаа гарвал:**

1. **Ctrl + Shift + Delete** дарах
2. "Cached images and files" сонгох
3. "Clear data" дарах
4. Браузер дахин ачаалах

### 2. Нэвтрэх

1. `/login` хуудас руу орох
2. **Имэйл**: `khashpay@gmail.com`
3. **Нууц үг**: Шинэ нууц үг (солигдсон нууц үг)
4. **"Нэвтрэх"** товч дарах

### 3. Хэрэв "Хэт олон оролдлого" алдаа гарвал

**15-30 минутын дараа дахин оролдох:**

Firebase Auth нь хэт олон буруу оролдлогод rate limiting хэрэглэдэг. 15-30 минутын дараа автоматаар сэргээнэ.

**Эсвэл Browser Cache цэвэрлэх:**

1. Browser cache цэвэрлэх (дээрх заавар)
2. Incognito/Private mode ашиглах
3. Дахин оролдох

---

## 🔍 Нэвтрэх Дараа Ажиллахгүй Байвал

### 1. Console дээр шалгах

Browser Console (F12) дээр:

```javascript
// User state шалгах
const user = JSON.parse(localStorage.getItem('user'));
console.log('User:', user);

// Firebase Auth state шалгах
import { auth } from '@/config/firebase';
console.log('Firebase Auth:', auth.currentUser);
```

### 2. Хуудас Refresh хийх

1. **F5** дарах (эсвэл хуудас refresh)
2. Нэвтрэх дахин оролдох

### 3. localStorage цэвэрлэх

Browser Console дээр:

```javascript
localStorage.clear();
window.location.reload();
```

---

## 📝 Хийгдсэн Өөрчлөлтүүд

1. **Login дараа хуудас refresh хийх** - `window.location.href` ашиглах
2. **queryClient.clear()** - Хуучин state цэвэрлэх
3. **Rate limiting алдааг илүү сайн handle хийх** - Тодорхой заавар өгөх

---

## 🆘 Хэрэв бүх зүйл зөв байгаа ч ажиллахгүй байвал

1. Browser cache цэвэрлэх
2. Incognito mode ашиглах
3. 15-30 минутын дараа дахин оролдох
4. Firebase Console дээр нууц үг шууд тохируулах

